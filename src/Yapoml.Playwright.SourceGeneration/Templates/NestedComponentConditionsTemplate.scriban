{{ for component in $1.components }}
{{if !component.is_plural && (component.by.segments == null || component.by.segments.size == 0) }}
public global::{{component.namespace}}.{{get_component_return_type component}}.ChainConditions {{component.name}}
{
	get
	{
		var byValue = "{{ escape component.by.value }}";

		{{ include 'ByTemplate' component }}

		var elementHandler = ResolveElementHandler("{{component.name}}", by, byFrom, "{{component.original_name}}");
		return new global::{{component.namespace}}.{{get_component_return_type component}}.ChainConditions(Timeout, PollingInterval, Driver, elementHandler, ElementLocator, EventSource, Logger, SpaceOptions) { Chain = Chain };
	}
}
{{ else if component.is_plural && (component.by.segments == null || component.by.segments.size == 0) }}
public global::{{component.namespace}}.{{get_component_return_type component}}List.Conditions {{component.name}}
{
	get
	{
		var byValue = "{{ escape component.by.value }}";

		{{ include 'ByTemplate' component }}

		var elementsListHandler = CreateElementsListHandler(by, byFrom, "{{singularize(component.original_name)}}", "{{component.original_name}}");
		return new global::{{component.namespace}}.{{component.singular_name}}ComponentList.Conditions(Timeout, PollingInterval, Driver, elementsListHandler, ElementLocator, EventSource, Logger, SpaceOptions) { Chain = Chain };
	}
}
{{ end }}
{{ end }}

{{ for component in $1.base_component?.components }}
{{if !component.is_plural && (component.by.segments == null || component.by.segments.size == 0) }}
public global::{{component.namespace}}.{{get_component_return_type component}}.ChainConditions {{component.name}}
{
	get
	{
		var byValue = "{{ escape component.by.value }}";

		{{ include 'ByTemplate' component }}

		var elementHandler = ResolveElementHandler("{{component.name}}", by, byFrom, "{{component.original_name}}");
		return new global::{{component.namespace}}.{{get_component_return_type component}}.ChainConditions(Timeout, PollingInterval, Driver, elementHandler, ElementLocator, EventSource, Logger, SpaceOptions) { Chain = Chain };
	}
}
{{ else if component.is_plural && (component.by.segments == null || component.by.segments.size == 0) }}
public global::{{component.namespace}}.{{get_component_return_type component}}List.Conditions {{component.name}}
{
	get
	{
		var byValue = "{{ escape component.by.value }}";

		{{ include 'ByTemplate' component }}

		var elementsListHandler = CreateElementsListHandler(by, byFrom, "{{singularize(component.original_name)}}", "{{component.original_name}}");
		return new global::{{component.namespace}}.{{component.singular_name}}ComponentList.Conditions(Timeout, PollingInterval, Driver, elementsListHandler, ElementLocator, EventSource, Logger, SpaceOptions) { Chain = Chain };
	}
}
{{ end }}
{{ end }}